Currently it's nessecary to compile each project separately, otherwise
compilation will fail. That's the reason why "Build Solution" doesn't build
both projects. To build the dedicated server executable just right-click 
the "Server" project and then click "Build".